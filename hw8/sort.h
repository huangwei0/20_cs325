#ifndef SORT
#define SORT
#include <fstream>
#include <iostream>
#include <limits>
#include <string>
#include <vector>

using std::ifstream;
using std::ofstream;
using std::string;
using std::vector;
using std::cout;
using std::endl;
void mergevalue(vector<int>&list11, int p, int q, int r);
void sort(vector<int>&list11, int p, int r);
#endif
